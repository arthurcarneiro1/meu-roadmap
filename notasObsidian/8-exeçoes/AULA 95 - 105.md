# 📝 Notas – Exceções em Java

## 1. Tipos de Exceções

- **Error** → Problemas graves relacionados à JVM (ex: `OutOfMemoryError`). Não devem ser tratados pela aplicação.
    
- **RuntimeException (Unchecked)** → Erros de lógica do programa. Não precisam ser tratados obrigatoriamente.
    
    - Ex: `NullPointerException`, `ArithmeticException`.
        
- **Exception (Checked)** → Precisam ser tratadas (`try/catch`) ou declaradas (`throws`).
    
    - Ex: `IOException`, `SQLException`.
        

---

## 2. Lançando Exceções

- **`throw`** → usado para lançar uma exceção dentro de um método.
    
    `throw new IllegalArgumentException("Idade inválida");`
    
- **`throws`** → usado na assinatura do método para indicar que ele pode lançar exceções.
    
    `void metodo() throws IOException { ... }`
    

---

## 3. Tratamento de Exceções

- **`try/catch`** → captura e trata exceções.
    
    `try {     int result = 10 / 0; } catch (ArithmeticException e) {     System.out.println("Erro: " + e.getMessage()); }`
    
- **Bloco `finally`** → sempre executado, com ou sem exceção.
    
    - Usado geralmente para fechar recursos.
        

---

## 4. Captura de Múltiplas Exceções

- **Blocos separados**:
    
    `try {     ... } catch (IOException e) {     ... } catch (SQLException e) {     ... }`
    
- **Multi-catch em linha** (Java 7+):
    
    `catch (IOException | SQLException e) {     ... }`
    

---

## 5. Try-with-Resources

- Fechamento automático de recursos (`AutoCloseable`).
    
    `try (BufferedReader br = new BufferedReader(new FileReader("file.txt"))) {     System.out.println(br.readLine()); } catch (IOException e) {     e.printStackTrace(); }`
    

---

## 6. Exceções Customizadas

- Criadas para representar regras específicas do domínio.
    
    `class IdadeInvalidaException extends Exception {     public IdadeInvalidaException(String msg) {         super(msg);     } }`
    

---

## 7. Sobrescrita e Exceções

- Métodos sobrescritos **não podem** lançar exceções mais restritivas do que os métodos da superclasse.
    
- Podem lançar exceções **iguais ou mais amplas** (checked e unchecked).